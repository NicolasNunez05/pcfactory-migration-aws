name: Blue-Green Deployment

# TEMPORALMENTE DESACTIVADO - El ALB no existe en la cuenta AWS
# Se reactivará cuando se asigne el quota
on:
  workflow_dispatch:  # ← Solo se ejecuta manualmente

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-1

jobs:
  blue-green:
    name: 🔵🟢 Blue-Green Deployment (DISABLED - No ALB)
    runs-on: ubuntu-latest
    
    steps:
      - name: ⚠️ Workflow Disabled
        run: |
          echo "Este workflow está desactivado porque la cuenta AWS no tiene cuota de ALB"
          echo "Solución: Solicitar aumento de cuota en AWS Service Quotas"
          exit 0
